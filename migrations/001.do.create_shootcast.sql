DROP TABLE IF EXISTS shootcast_lists;

CREATE TABLE shootcast_lists (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL
);

DROP TABLE IF EXISTS shootcast_cities;

CREATE TABLE shootcast_cities (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  name TEXT NOT NULL
);

ALTER TABLE shootcast_cities
  ADD COLUMN list_id INTEGER REFERENCES shootcast_lists( id ) ON DELETE CASCADE NOT NULL;